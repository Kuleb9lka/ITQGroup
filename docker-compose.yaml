services:
  document-db:
    image: postgres:17
    container_name: document-db
    environment:
      POSTGRES_DB: itqgroupdb
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:ata